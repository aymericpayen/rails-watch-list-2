<div class="container">
  <h1>New of bookmarks</h1>
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.input :comment, placeholder: 'Drama' %>
    <%= f.association :movie, collection: Movie.all %>
    <%= f.submit class: 'btn btn-success' %>
  <% end %>
</div>
